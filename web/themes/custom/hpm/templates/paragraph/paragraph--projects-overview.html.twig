{#
/**
 * @file
 * Projects overview – embeds the projects_listing View (with BEF filter links).
 *
 * The View renders project teasers in a CSS grid via its own templates.
 * Editors add intro content (meta label, blockquote) as separate paragraphs
 * above this one on the page node.
 */
#}
<div{{ attributes }}
  {% if theme_css %}data-theme="{{ theme_css }}"{% endif %}>
  {{ drupal_view('projects_listing', 'block_1') }}
</div>
