{#
/**
 * @file
 * Job intro – intentionally empty.
 *
 * The job_intro paragraph data is consumed directly in node--job-listing.html.twig
 * (fallback section). This template renders nothing to avoid duplicate output.
 */
#}
