{#
/**
 * @file
 * Field template for field_headline – renders without wrapper markup.
 *
 * Plain string fields used inline inside headings must not have the
 * default <div> wrapper that field.html.twig adds.
 */
#}
{% for item in items %}{{ item.content }}{% endfor %}